# Quy tắc bắt buộc cho MỌI phiên làm việc trong repo này

## Nhật ký làm việc (`docs/NHAT-KY-LAM-VIEC.md`)

- **Đầu phiên (bắt đầu 1 phiên mới, kể cả sau mỗi ~5 tiếng làm việc liên tục):** đọc lại
  `docs/NHAT-KY-LAM-VIEC.md` — ưu tiên đọc phần cuối file (mục ngày gần nhất) trước khi bắt đầu bất
  kỳ việc gì, để không lặp lại việc đã làm hoặc quên quyết định/lỗi đã ghi nhận trước đó.
- **Cuối phiên (kết thúc 1 phiên, kể cả khi hết ~5 tiếng làm việc liên tục hoặc dừng giữa chừng):**
  cập nhật `docs/NHAT-KY-LAM-VIEC.md` với việc đã làm trong phiên — theo đúng khuôn đã có sẵn ở đầu
  file (ngày, việc đã làm, kết quả, việc còn dở). Không đợi Minh nhắc.
- Nếu phát hiện lỗi QUY TRÌNH (không phải lỗi code) gây lãng phí thời gian/tiền — vd gọi AI lặp lại
  không cần thiết, khởi động lại nhiều lần để thử từng tham số, quên tận dụng cơ chế đã có sẵn — ghi
  rõ thành mục "BÀI HỌC QUY TRÌNH" trong nhật ký, không chỉ ghi kết quả cuối cùng.
- Lý do (Minh, 2026-08-19): nhật ký từng bị bỏ quên 6 ngày liên tục đúng lúc khối lượng việc/chi phí
  tăng cao nhất — không có bản ghi tức thời khiến lỗi quy trình (gọi AI trùng lặp, khởi động lại lãng
  phí) không bị phát hiện sớm.
